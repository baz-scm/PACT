{
  "name": "@baz-scm/pact-hooks",
  "version": "0.1.0",
  "description": "Claude Code + Cursor hooks for PACT plan capture",
  "files": [
    "dist",
    "hooks",
    ".claude-plugin",
    "src"
  ],
  "bin": {
    "pact-capture": "dist/cc-capture.js",
    "pact-gate": "dist/cc-gate.js",
    "pact-nudge": "dist/cc-nudge.js",
    "pact-hooks": "dist/install.js"
  },
  "scripts": {
    "build": "node scripts/build.mjs && node scripts/postbuild.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.28.1",
    "tsx": "^4.0.0",
    "typescript": "^5.8.0",
    "vitest": "^3.0.0"
  }
}
