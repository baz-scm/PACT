{
  "name": "@baz-scm/pact-hooks",
  "version": "0.0.1",
  "description": "Claude Code + Cursor hooks for PACT plan capture",
  "files": ["dist", "hooks", ".claude-plugin"],
  "bin": {
    "pact-capture": "dist/src/cc-capture.js",
    "pact-nudge": "dist/src/cc-nudge.js",
    "pact-hooks": "dist/src/install.js"
  },
  "scripts": {
    "build": "tsc && node scripts/postbuild.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.8.0",
    "vitest": "^3.0.0"
  }
}
